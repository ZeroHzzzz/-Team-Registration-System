export interface Message {
    Username: string,
    Messagetype: number,
    TeamID: number
}
export interface card {
    Username: string,
    Usercard: string,
    Userdescription: string
}